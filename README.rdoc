== Description

This is the application for Travel

== Setup for development

1. Run 'bundle' and 'rake db:create'

2. Copy config/applicatoin.yml.sample to config/application.yml and change the settings(host, twitter/facebook api keys)

4. Install imagemgick for image processing
    Command in ubuntu: sudo apt-get install imagemagick
    Command in Mac: 

5. Run 'rails server'

6. Go to http://localhost:3000/ and you'll see 1 page.

7. That's all yet. ;)
